# New Chat Start Here

Upload the complete `VisularAITerms_Handoff_v0.5.0.zip` to a new ChatGPT conversation and use a request like:

> Continue development of **Visular AI Terms / Concepts** from the attached handoff ZIP. First inspect `HANDOFF.md`, `AGENTS.md`, the authoritative docs, and the actual project files. Treat v0.5.0 as the current known-good baseline. Preserve the complete-ZIP and root `start-server.bat` delivery requirements. Preserve the Iteration 4 browser-local Saved/Recent/Compare behavior and do not add translation functionality unless I explicitly request it. Before changing code, summarize your understanding of the current state and requested next scope.

The handoff ZIP contains the full application, so no previous project ZIP is required.
